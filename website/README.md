# =============================================================
# Cloudflare Pages 部署配置
#
# 在 Cloudflare Dashboard 设置:
#   Build command:    npm run build
#   Build output dir: dist
#   Root directory:   website
#   Node version:     22
#
# 或用 Wrangler CLI:
#   cd website && wrangler pages deploy dist --project-name education-advisor
# =============================================================
