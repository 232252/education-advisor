// =============================================================
// feishu-bot/dedup-cache — 兼容壳(M2)
// 实现已上提到 channels/runtime/dedup-cache.ts(平台无关,默认容量 500)。
// =============================================================

export { MessageDedupCache } from '../channels/runtime/dedup-cache'
