// =============================================================
// feishu-bot/recent-files — 兼容壳(M2)
// 实现已上提到 channels/runtime/recent-files.ts(平台无关,默认 5 个/30 分钟)。
// =============================================================

export {
  type RecentFile,
  type RecentFilesOptions,
  RecentFilesStore,
} from '../channels/runtime/recent-files'
