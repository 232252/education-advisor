// =============================================================
// feishu-bot/streaming-card — 兼容壳(M3)
// 实现已搬至 channels/adapters/feishu/reply-session.ts;
// ReplySession 接口契约在 @shared/types/channel。
// =============================================================

export {
  type ReplySession,
  type ReplySessionDeps,
  createReplySession,
} from '../channels/adapters/feishu/reply-session'
